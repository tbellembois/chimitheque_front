use egui::CornerRadius;

// Layouts.
pub const VISUALS_WIDGETS_ACTIVE_CORNER_RADIUS: CornerRadius = CornerRadius::same(5);
pub const VISUALS_WIDGETS_HOVERED_CORNER_RADIUS: CornerRadius = CornerRadius::same(5);
pub const VISUALS_WIDGETS_INACTIVE_CORNER_RADIUS: CornerRadius = CornerRadius::same(5);
pub const VISUALS_WIDGETS_NONINTERACTIVE_CORNER_RADIUS: CornerRadius = CornerRadius::same(5);
pub const VISUALS_WIDGETS_OPEN_CORNER_RADIUS: CornerRadius = CornerRadius::same(5);

pub const GLOBAL_FONT_SIZE: f32 = 18.0;
